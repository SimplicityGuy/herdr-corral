/**
 * Placeholder for the schema generator (bead: schema).
 *
 * It will fetch https://herdr.dev/docs/config-reference/, parse each
 * `<li class="config-reference-row">` into a setting, and write
 * src/schema/reference.json. Until that bead lands this exits 0 so
 * `pnpm gen:reference` is a real, wired-up script rather than a missing one.
 */
console.log('TODO(schema bead): generate src/schema/reference.json from herdr.dev')
